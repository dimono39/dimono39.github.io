# 📚 API Документация системы анализа образовательных результатов

## 🏗️ Архитектура

### Основные модули:

1. **DataManager** - управление данными приложения
2. **TemplateScanner** - работа с шаблонами тестов
3. **ChartRenderer** - визуализация данных
4. **ExportManager** - экспорт отчетов
5. **ErrorHandler** - обработка ошибок
6. **TableRenderer** - рендеринг таблиц

## 📊 DataManager API

### Класс: `DataManager`

```javascript
/**
 * Главный менеджер данных приложения
 * @class
 */
class DataManager {
  /**
   * Загружает данные из localStorage
   * @returns {AppData} Структура данных приложения
   */
  loadData() {}
  
  /**
   * Сохраняет данные в localStorage
   * @param {AppData} data - Данные для сохранения
   */
  saveData(data) {}
  
  /**
   * Синхронизирует массивы данных
   * Гарантирует корректность структуры
   */
  syncData() {}
}